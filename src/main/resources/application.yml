spring:
  application:
    name: order-payment-service
  config:
    import: optional:classpath:properties/env.properties

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      database: ${REDIS_DATABASE}

  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}


 #이메일설정
  mail:
    host: smtp.gmail.com
    port: 587
    username: j01099209066@gmail.com
    password: rwaapcyxrgbhgwls
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true



tosspayments:
  client-key: ${TOSS_CLIENT_KEY}
  secret-key: ${TOSS_SECRET_KEY}




